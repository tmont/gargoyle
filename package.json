{
	"name": "gargoyle",
	"version": "0.3.6",
	"description": "Watches a directory for file changes. You know. Like a gargoyle.",
	"keywords": [ "watch", "watcher" ],

	"author": "Tommy Montgomery <tmont@tmont.com> (http://tmont.com/)",
	"repository": {
		"type": "git",
		"url": "https://github.com/tmont/gargoyle.git"
	},
	"files": [ "index.js" ],

	"dependencies": {
		"async": "1.4.2"
	},

	"devDependencies": {
		"mocha": "2.3.3",
		"should": "7.1.0",
		"fs-extra": "0.24.0"
	},

	"scripts": {
		"test": "node_modules/.bin/mocha -R spec -t 5000 tests"
	}
}